# v0.8.9 - Strategy pill relabel

The strategy segmented pill on the Install tab now reads
**Simple / Complex** instead of `pm-disable / HDB broker`.
Internal strategy names (`pm_disable_install`,
`hdb_broker_install`), settings keys, and code references are
unchanged - this is a UI label change only, to keep the surface
friendlier without shifting any behavior.
